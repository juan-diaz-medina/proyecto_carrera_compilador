{
  "name": "mindar-compiler-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "vercel-build": "echo 'Build completo'",
    "postinstall": "npx playwright install chromium" 
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "playwright-core": "^1.48.0"
  }
}
